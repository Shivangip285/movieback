CREATE TABLE PASSWORDLOGGER (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
    USER_ID BIGINT,
    USER_NAME VARCHAR(255) NOT NULL,
    OLDER_PASSWORD  VARCHAR(255),
    OLDEST_PASSWORD  VARCHAR(255),
    PRIMARY KEY (ID),
    FOREIGN KEY (USER_ID) REFERENCES USERTABLE(ID)
);